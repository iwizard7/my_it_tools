services:
  gateway:
    build: .
    ports:
      - "8080:8080"
    environment:
      GATEWAY_DATA_DIR: /app/data
    volumes:
      - gateway-data:/app/data
    restart: unless-stopped

volumes:
  gateway-data:
